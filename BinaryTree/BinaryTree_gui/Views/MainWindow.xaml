<Window x:Class="BinaryTree_gui.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BinaryTree" MinHeight="200" Width="200"
        SizeToContent="WidthAndHeight">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0">
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="File">
                    <MenuItem Header="Open" Click="OpenFileItem_OnClick"/>
                    <MenuItem Header="Save as image" Click="SaveAsImageItem_OnClick"/>
                    <Separator />
                    <MenuItem Header="Exit" Click="ExitItem_OnClick"/>
                </MenuItem>
                <MenuItem Header="Restore" Click="RestoreItem_OnClick"/>
            </Menu>
        </DockPanel>
        <ScrollViewer Name="BinaryTreeCanvasScrollViewer" Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"/>

    </Grid>
</Window>