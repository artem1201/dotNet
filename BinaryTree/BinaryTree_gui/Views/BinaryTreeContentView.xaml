<StackPanel x:Class="BinaryTree_gui.Views.BinaryTreeContentView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            mc:Ignorable="d">

    <StackPanel.Resources>
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="10" />
        </Style>
    </StackPanel.Resources>
    <StackPanel Orientation="Horizontal">
        <TextBlock Text="Name:" Style="{StaticResource HeaderStyle}" />
        <TextBlock Name="NodeName" Style="{StaticResource HeaderStyle}" Foreground="Blue" />
    </StackPanel>
    <TextBlock Name="NodeContent" Margin="5" />
    <Separator />
    <StackPanel Name="AttributesPanel">
        <TextBlock Text="Attributes" Style="{StaticResource HeaderStyle}" HorizontalAlignment="Center" />
    </StackPanel>
</StackPanel>